package WizardTD.Gameplay.Tiles;

import lombok.*;

/**
 * Placeholder tile that represents a spot where a wizard should go
 */
@ToString
@EqualsAndHashCode(callSuper = false)
public final class WizardHousePlaceholder extends InvalidTile {
//    @Override
//    public void render(@NonNull final PApplet app, final float centreX, final float centreY) {
//        // Unicorn vomit lol
//        final var img = new PImage(1, 1, PConstants.ARGB);
//        img.set(0, 0, ThreadLocalRandom.current().nextInt(0, Integer.MAX_VALUE));
//        img.resize(GuiConfig.CELL_SIZE_PX, GuiConfig.CELL_SIZE_PX);
//        UiManager.renderSimpleTile(app, img, centreX, centreY);
//    }
}